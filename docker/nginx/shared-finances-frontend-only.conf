server {
    listen ${SF_APP_PORT};
    listen [::]:${SF_APP_PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    include /etc/nginx/snippets/shared-finances-static.conf;
}
